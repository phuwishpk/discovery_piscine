#!/usr/bin/env python3
import sys

# Get the number of parameters passed to the script (excluding the script name itself)
num_parameters = len(sys.argv) - 1

# Display the number of parameters
print(f"Number of parameters: {num_parameters}.")

